import { CrudListColumnConfig } from './crud-list-column-config';

describe('CrudListColumnConfig', () => {
  it('should create an instance', () => {
    expect(new CrudListColumnConfig()).toBeTruthy();
  });
});
