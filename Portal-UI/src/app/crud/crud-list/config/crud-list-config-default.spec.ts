import { CrudListConfigDefault } from './crud-list-config-default';

describe('CrudListConfigDefault', () => {
  it('should create an instance', () => {
    expect(new CrudListConfigDefault()).toBeTruthy();
  });
});
