import { CrudDialogFieldConfig } from './crud-dialog-field-config';

describe('CrudDialogFieldConfig', () => {
  it('should create an instance', () => {
    expect(new CrudDialogFieldConfig()).toBeTruthy();
  });
});
