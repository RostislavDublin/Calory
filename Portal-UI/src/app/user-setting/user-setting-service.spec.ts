import { UserSettingService } from './user-setting-service';

describe('UserSettingService', () => {
  it('should create an instance', () => {
    expect(new UserSettingService()).toBeTruthy();
  });
});
